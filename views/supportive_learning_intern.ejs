<!DOCTYPE html>
<html ng-app="MainApp">
<head>
    <% include partials/head %>
    <script src="/javascripts/controllers/InternObservation.js" type="text/javascript"></script>
</head>
<body>
<header>
    <% include partials/admin_header %>
</header>
<% include partials/slideout_menu %>

<div class="container" ng-controller="InternObservationController as observation">
    <main>
        <div class="row">
            <h4 class="col s12"><%= title %></h4>
        </div>
        <!--TODO I think we need to use UserID instead of InternID-->
        <div class="row">
            <table class="striped col s12 radioTable">
                <th colspan="4">
                    Interns
                </th>
                <tr ng-repeat="Intern in observation.internData track by $index">
                    <td>
                        {{Intern.FirstName}} {{Intern.LastName}}
                    </td>
                    <td>
                        <input id="{{'OnTarget' + Intern.InternID}}" class="with-gap" type="radio"
                               ng-model="Intern.ObsValue" value="OnTarget" name="{{Intern.InternID}}">
                        <label for="{{'OnTarget' + Intern.InternID}}">On target</label>
                    </td>
                    <td>
                        <input id="{{'Enrichment' + Intern.InternID}}" class="with-gap" type="radio"
                               ng-model="Intern.ObsValue" value="Enrichment" name="{{Intern.InternID}}">
                        <label for="{{'Enrichment' + Intern.InternID}}">Enrichment</label>
                    </td>
                    <td>
                        <input id="{{'Support' + Intern.InternID}}" class="with-gap" type="radio"
                               ng-model="Intern.ObsValue" value="Support" name="{{Intern.InternID}}">
                        <label for="{{'Support' + Intern.InternID}}">Support</label>
                    </td>
                </tr>

            </table>

            <button class="right btn-small waves-effect waves-light btn" id="observationButton"
                    type="button" ng-click="observation.saveObs('<%= title %>')">SAVE
            </button>
            <span class="saved right" ng-if="observation.obsSaved">{{ observation.message }}</span>
        </div>

    </main>

</div>

</body>
</html>